# Ethode Lunch and Learn Project 1

## Installation
1. Get a LAMP or WAMP stack running
1. Copy repo files into designated HTTP server root
1. Configure database and .env
1. Make sure php and composer are set in environment variables
1. reset migrations `php artisan migrate:refresh` (to seed with faker, add --seeder=IdeaSeeder)